class UserModel {
  String avatarUrl;
  String firstName;
  String lastName;
  bool isNotification;
  bool isPremium;

  UserModel(this.avatarUrl, this.firstName, this.lastName, this.isNotification, this.isPremium);
}
