class UserForm {
  UserForm(this.avatarUrl, this.firstName, this.lastName, this.isNotification, this.isPremium);

  String avatarUrl;
  String firstName;
  String lastName;
  bool isNotification;
  bool isPremium;
}
